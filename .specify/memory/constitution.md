<!--
  Sync Impact Report

  Version change: 1.0.0 → 1.1.0 (PATCH→MINOR: new principle added)
  Modified principles: N/A
  Added sections:
    - VI. Documentation Language (English) — new principle
  Removed sections: N/A
  Templates requiring updates:
    - .specify/templates/plan-template.md — ✅ reviewed, no changes needed
    - .specify/templates/spec-template.md — ✅ reviewed, no changes needed
    - .specify/templates/tasks-template.md — ✅ reviewed, no changes needed
    - .specify/templates/checklist-template.md — ✅ reviewed, no changes needed
  Follow-up TODOs: None
-->

# opencode_angular Constitution

## Core Principles

### I. Scaffolding-First

All project initialization MUST begin with `ng new` using the Angular CLI.
Use `--standalone --routing --style=scss` flags. The `angular-new-app` skill
MUST be loaded before scaffolding to ensure modern Angular defaults. No
hand-written project wiring, no manual `package.json` at root, no
bypassing the CLI generator.

### II. Standalone Architecture

All components, directives, and pipes MUST be standalone. No NgModules
SHOULD be created except those auto-generated by the Angular CLI for
routing or lazy-loading boundaries. Bootstrap MUST use
`bootstrapApplication` — never `NgModule`-based bootstrap. The
`angular-developer` skill MUST be loaded before writing or modifying
application code.

### III. Signals-First Reactivity (PREFERRED)

Angular signals MUST be the default mechanism for state management and
change detection. Prefer `signal`, `computed`, `linkedSignal`, `resource`,
and `effect` over `RxJS`-based patterns unless an external API or library
requires observables. New code SHOULD NOT depend on `NgZone` change
detection for internal state flow.

### IV. Test-First (NON-NEGOTIABLE)

Tests MUST be written and observed to fail before implementation code is
written. Every component, service, directive, and pipe MUST have at least
one test covering its primary behavior. Angular TestBed with Jasmine or
Jest (as determined by the project's `angular.json` unitTestFramework)
MUST be used. Integration tests MUST be added for any feature involving
HTTP, routing, or inter-component communication.

### V. TypeScript Strict Mode

All code MUST pass strict TypeScript checks. The `any` type MUST NOT be
used unless explicitly justified in a comment. Angular typed forms
(`FormGroup<...>`, `FormControl<...>`) MUST be used over untyped forms.
Strict template type checking MUST remain enabled in `tsconfig.json`.

### VI. Documentation Language (English)

All project documentation MUST be written in English. This includes
specifications, implementation plans, task lists, checklists, README,
code comments, and commit messages. Non-English documentation and
comments MUST NOT be committed to the repository.

## Technology Stack & Constraints

- **Documentation Language**: English (all specs, plans, tasks, comments)
- **Framework**: Angular (latest stable, installed via Angular CLI)
- **Language**: TypeScript with strict mode enabled
- **Styling**: SCSS (component-scoped styles)
- **Architecture**: Standalone components, signals-first, routing
- **Project State**: Pre-scaffolding — no `src/` or root `package.json`
- **CLI Tooling**: Angular CLI via local MCP server
- **SDD Workflow**: Speckit — `specify → plan → tasks → implement`
- **Context Source**: `AGENTS.md` (designated via `.specify/init-options.json`)
- **Secrets**: `.secrets/github-token` and `.secrets/context7-key` are
  gitignored; MCP failures likely mean a missing or empty secret file
- **Deleted Artifacts**: `.squad/` and `.copilot/` have been removed;
  ignore any references to squad agents or copilot skills from old docs

## Development Workflow

All feature development MUST follow the Speckit SDD cycle:
1. `speckit.specify` — write feature specification with user stories
2. `speckit.plan` — create implementation plan with technical research
3. `speckit.tasks` — break plan into sequenced, independently testable tasks
4. `speckit.implement` — execute tasks in order (tests before code)

Feature branches MUST be created via `speckit.git.feature`. Commits
MUST be made with `speckit.git.commit` after each logical unit of work.
`AGENTS.md` is the single source of agent guidance and MUST be updated
whenever project conventions change.

## Governance

This constitution supersedes any ad-hoc practices. Amendments require:
1. A documented change in the Sync Impact Report at the top of this file
2. Version bump per semantic versioning rules (MAJOR for incompatible
   governance changes, MINOR for new principles, PATCH for clarifications)
3. Propagation review across all templates in `.specify/templates/`

All pull requests and reviews MUST verify compliance with these principles.
Violations MUST be documented in the Constitution Check section of
`plan.md` with a written justification and rejected simpler alternative.

**Version**: 1.1.0 | **Ratified**: 2026-05-24 | **Last Amended**: 2026-05-24
